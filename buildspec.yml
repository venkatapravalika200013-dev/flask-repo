version 0.2

phases:
  install:
    runtime-versions:
       python: 3.10
    commands:
      - echo "Installing dependencies..."
      - pip install -r requirements.txt -t
      
  build:
    commands:
      - echo "Packaging app..."
      - zip -r flask_app.zip

artifacts:
  files:
    - flask_app.zip
